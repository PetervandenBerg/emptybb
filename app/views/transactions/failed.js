Wrts.views.Transactions.Failed = Wrts.BaseView.extend({
  templateName: 'transactions/failed',
  decorator: Wrts.decorators.Transactions.Failed,

  init: function() {
    this.render();
  },

  events: {
  },

  // Event functions
  // ===================================
  //

});
